import { mostAnswers } from 'message-board2/helpers/most-answers';
import { module, test } from 'qunit';

module('Unit | Helper | most answers');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = mostAnswers([42]);
  assert.ok(result);
});
